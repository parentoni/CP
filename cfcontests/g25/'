#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define ar array

int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);
  freopen("shell.in", "r", stdin);
  int n;
  cin >> n;
  int a[n][3];
  for (int i=0;i<n;i++){
    int a,b,g;
    cin >> a >> b >> g;
    a[i][0] = a;


  freopen("shell.out", "w", stdout);
  cout << i << endl;
}

